name: Combined Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # Email step
      - name: 23f2002963@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      # Cache step
      - name: Prime cache
        uses: actions/cache@v4
        with:
          path: node_modules
          key: cache-b88b72c
      - name: prime-cache-b88b72c
        run: echo "Cache primed or hit/miss result"
